import 'dart:async';
import 'package:cuartup/models/user.dart';

class RegisterDataProvider {
  Future<void> register(User user) async {
    // Lógica para registrar un nuevo usuario
    // Puedes utilizar una API o una base de datos para almacenar los datos del usuario
    // Por simplicidad, este ejemplo no realiza ninguna operación
  }
}
